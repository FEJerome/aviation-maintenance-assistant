# 根目录通用忽略
.DS_Store
*.log

# 兜底：防止在根目录误执行 Maven 时产生 target/
target/

# 后端（通过 backend/.gitignore 处理）

# 前端
frontend/node_modules/
frontend/dist/
frontend/.vite/

# 测试截图（报告正文保留，图片不入库）
docs/test-reports/*.png
